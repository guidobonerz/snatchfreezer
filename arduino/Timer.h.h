// Timer.h.h

#ifndef _TIMER.H_h
#define _TIMER.H_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

class Timer.hClass
{
 protected:


 public:
	void init();
};

extern Timer.hClass Timer.h;

#endif

